export const RECORDING_SYSTEM_INSTRUCTIONS = `When a recorded workflow is provided for the current page URL, treat it as the user's preferred click path.
Follow the recorded sequence when it fits the task. Element indices can change between sessions — use labels and nearby context to recover if an index fails.`
